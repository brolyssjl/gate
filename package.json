{
  "name": "gate-cli",
  "version": "1.0.0",
  "private": true,
  "description": "Agent-agnostic quality harness: turns the dev flow into an enforced state machine with deterministic gates.",
  "keywords": [
    "ai",
    "agent",
    "quality",
    "gate",
    "state-machine",
    "claude",
    "cursor",
    "agents.md",
    "coding-agent",
    "cli"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/brolyssjl/gate.git"
  },
  "type": "module",
  "bin": {
    "gate": "dist/cli.js"
  },
  "files": [
    "dist",
    "playbooks"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/embedPlaybooks.mjs && tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "conformance": "vitest run test/cli.e2e.test.ts test/concurrency.test.ts test/humanReview.test.ts test/prune.test.ts test/guard.test.ts test/amend.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
