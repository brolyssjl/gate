[package]
name = "gate"
version = "1.0.0"
edition = "2021"
description = "Agent-agnostic quality harness: turns the dev flow into an enforced state machine with deterministic gates."
license = "MIT"
publish = false

[[bin]]
name = "gate"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = 3
