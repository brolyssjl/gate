[package]
name = "gate"
version = "1.1.0"
edition = "2021"
description = "Agent-agnostic quality harness: turns the dev flow into an enforced state machine with deterministic gates."
license = "MIT"
repository = "https://github.com/brolyssjl/gate"
keywords = ["cli", "quality-gate", "ai-agents", "devflow", "workflow"]
categories = ["command-line-utilities", "development-tools"]
publish = false

[[bin]]
name = "gate"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = 3
