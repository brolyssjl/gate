//! Skeleton for `src/artifacts/frontmatter.ts` (`splitFrontmatter`, the
//! shared YAML-frontmatter split used by every artifact parser). Ported in
//! wave 3 (see `docs/rust-port.md`), built on `core::yaml` (wave 1, already
//! covers the block-sequence-of-maps shapes this needs - see its module
//! doc comment).
