//! Skeleton for `src/cli/context.ts` (`requireRoot`, `requireActiveRun`,
//! `ActiveContext`). Owned by wave 4 per `docs/rust-port.md`'s wave plan
//! ("the 17 commands + cli/context + commands/shared") - it depends on
//! `core::run` and `core::current` (wave 2), so it is left unimplemented
//! here rather than partially ported. No wave-1 code calls into this
//! module (every command is a stub).
