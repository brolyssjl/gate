//! Skeleton for `src/commands/advance.ts` (`advance`: moves a run to its
//! next phase and records the transition). Ported alongside the commands
//! that call it (wave 4, see `docs/rust-port.md`).
