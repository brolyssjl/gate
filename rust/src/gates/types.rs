//! Skeleton for `src/gates/types.ts` (`Check`, `GateResult`, `GateContext`,
//! the `Gate` fn type, `pass`/`fail`/`result` helpers). Ported in wave 3
//! (see `docs/rust-port.md`), once `core::run::Run` (wave 2) exists for
//! `GateContext` to hold.
