//! Skeleton for `src/integrations/agnosgramWrite.ts` (`formatJournalEntry`,
//! journal write via the `agnosgram` CLI with a fallback). Ported in wave 3
//! (see `docs/rust-port.md`), via `std::process::Command`.
